from brain.command_risk_scanner import (
    CommandRiskScanner
)


CommandRiskScanner.show_scan(
    "dir"
)

CommandRiskScanner.show_scan(
    "pip install requests"
)

CommandRiskScanner.show_scan(
    "format C:"
)

CommandRiskScanner.show_scan(
    "shutdown /s"
)