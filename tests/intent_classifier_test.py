from brain.intent_classifier import (
    IntentClassifier
)


IntentClassifier.show_intent(
    "Build an independent AI operating system"
)

IntentClassifier.show_intent(
    "What is CPU usage?"
)

IntentClassifier.show_intent(
    "Remember Phase 4 progress"
)

IntentClassifier.show_intent(
    "Analyze this dataset"
)

IntentClassifier.show_intent(
    "Open a file"
)