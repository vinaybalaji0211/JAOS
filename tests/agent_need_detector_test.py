from brain.agent_need_detector import AgentNeedDetector

detector = AgentNeedDetector()

detector.show_analysis(
    "Need code review"
)

detector.show_analysis(
    "Need pet care assistant"
)

detector.show_analysis(
    "Need finance planning agent"
)